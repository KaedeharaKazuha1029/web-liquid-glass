# Claude Code instructions

Read and follow [`AGENTS.md`](./AGENTS.md) before changing or integrating this project.

For navigation bars, prefer the simplified `liquidGlass()` interface with the built-in canonical parameters. Do not copy the demo implementation, substitute a CSS approximation, or create an unrelated visual parameter set unless the user explicitly asks for that change.
